<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Pantry Item</title>
</head>
<body>
  <%- include('../partials/_navbar.ejs') %>
  <h1>Edit Pantry Item</h1>

  <form action="/users/<%= user._id %>/foods/<%= foodItem._id %>?_method=PUT" method="POST">
    <label for="name">Food Name:</label>
    <input type="text" id="name" name="name" value="<%= foodItem.name %>" required>

    <label for="quantity">Quantity:</label>
    <input type="number" id="quantity" name="quantity" value="<%= foodItem.quantity %>" required>

    <label for="expiryDate">Expiry Date:</label>
    <input type="date" id="expiryDate" name="expiryDate" value="<%= new Date(foodItem.expiryDate).toISOString().split('T')[0] %>" required>

    <button type="submit">Update Item</button>
  </form>
</body>
</html>
